function soma(n1=0, n2=0) { // Parâmetro n1 e parametro n2=0, valor dele sera atribuido abaixo 
    return n1 + n2 // Retorne n1 + n2
}

console.log(soma(7)) // Nesse console.log nos definimos o valor que utilizaremos para os parâmetros da função acima